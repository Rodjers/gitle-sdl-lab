# gitle-sdl-lab

## Dependencies
* cmake
* gcc

## Build

```
git clone https://github.com/Rodjers/gitle-sdl-lab
cd gitle-sdl-lab
git submodule init
git submodule update
cmake -S . -B build
cmake --build build
```

